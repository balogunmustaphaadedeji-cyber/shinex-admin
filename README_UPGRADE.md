# SHINEX Admin upgrade

Standalone admin frontend. It remains separate from the user app and uses the existing backend with server-side admin authorization. Added screens: subscriptions, listing tags and policies/content; product moderation actions and stored contact replies are wired to the upgraded API.
