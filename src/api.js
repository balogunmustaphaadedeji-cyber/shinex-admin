const BASE=(import.meta.env.VITE_SHINEX_API_URL||'https://shinex-marketplace.onrender.com/api').replace(/\/$/,'');
export const token={get:()=>sessionStorage.getItem('shinex_admin_token'),set:t=>sessionStorage.setItem('shinex_admin_token',t),clear:()=>sessionStorage.removeItem('shinex_admin_token')};
export async function api(path,{method='GET',body,form=false}={}){const h={};const t=token.get();if(t)h.Authorization=`Bearer ${t}`;if(body&&!form)h['Content-Type']='application/json';let r;try{r=await fetch(BASE+path,{method,headers:h,body:form?body:body?JSON.stringify(body):undefined})}catch{const e=new Error('Network error. Check your connection and the SHINEX API URL.');e.status=0;throw e}let j=null;try{j=await r.json()}catch{}if(!r.ok){const e=new Error(j?.message||`Request failed (${r.status})`);e.status=r.status;throw e}return j}
export const money=n=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',maximumFractionDigits:0}).format(Number(n)||0);export const date=s=>s?new Intl.DateTimeFormat('en-NG',{dateStyle:'medium',timeStyle:'short'}).format(new Date(s)):'—';
