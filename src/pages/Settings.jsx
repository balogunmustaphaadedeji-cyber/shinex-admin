import {useTheme} from '../theme';export default function Settings(){const[t,setT]=useTheme();return <div className="content narrow"><div className="page-title"><span className="eyebrow">Preferences</span><h1>Settings</h1></div><div className="panel setting"><div><b>Theme</b><span>Only the admin theme preference is stored locally.</span></div><select value={t} onChange={e=>setT(e.target.value)}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></div></div>}