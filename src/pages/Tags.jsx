import React,{useEffect,useState} from 'react';import {api} from '../lib/api';
export default function Tags(){const [tags,setTags]=useState([]),[name,setName]=useState('');const load=()=>api('/admin/tags').then(r=>setTags(r.data||[]));useEffect(()=>{load()},[]);const add=async()=>{if(!name.trim())return;await api('/admin/tags',{method:'POST',body:{name}});setName('');load()};const toggle=async t=>{await api(`/admin/tags/${t.id}`,{method:'PATCH',body:{is_active:!t.is_active}});load()};return <div><h1 className="text-2xl font-bold text-gray-900 mb-5">Listing tags</h1><div className="flex gap-2 mb-5"><input className="border rounded-lg px-3 py-2" placeholder="New tag" value={name} onChange={e=>setName(e.target.value)}/><button className="bg-green-700 text-white rounded-lg px-4" onClick={add}>Add</button></div><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-3">{tags.map(t=><div key={t.id} className="bg-white border rounded-xl p-3 flex justify-between"><span>{t.name}</span><button className="text-sm" onClick={()=>toggle(t)}>{t.is_active?'Disable':'Enable'}</button></div>)}</div></div>}
