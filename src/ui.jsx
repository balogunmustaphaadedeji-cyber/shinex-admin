import {Link} from 'react-router-dom';import Icon from './icon';export function Logo(){return <Link className="brand" to="/"><img src="/shinex-icon.svg"/><span><b>SHINEX</b><small>Admin</small></span></Link>}export function Button({children,variant='primary',...p}){return <button className={`btn ${variant}`} {...p}>{children}</button>}export function State({type='empty',title,body,children}){return <div className={`state ${type}`}><div className="state-symbol">{type==='error'?'!':'S'}</div><h3>{title}</h3>{body&&<p>{body}</p>}{children}</div>}export function Avatar({row}){return <div className="avatar">{row?.avatar_url?<img src={row.avatar_url} alt=""/>:<span>{(row?.full_name||row?.username||'U').slice(0,1).toUpperCase()}</span>}</div>}
